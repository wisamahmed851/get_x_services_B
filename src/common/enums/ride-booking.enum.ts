export enum RideType {
  PRIVATE = 'private',
  CARPOOL = 'carpool',
}

export enum RideStatus {
  BOOKED = 'booked',
  ARRIVED = 'arrived',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
}
