export enum RideType {
  PRIVATE = 'private',
  CARPOOL = 'carpool',
}

export enum RideStatus {
  BOOKED = 'booked',
  ACCEPTED = 'ACCEPTED',
  ARRIVED = 'arrived',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
}
